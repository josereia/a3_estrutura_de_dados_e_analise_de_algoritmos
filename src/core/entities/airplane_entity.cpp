#include <string>

using namespace std;

namespace Entities {
struct airplane_entity {
  int id;
  string model;
  string manufacturer;
  int capacity;
  float length;
  float height;
  float speed;
  float altitude;
  float engine;
};
}  // namespace Entities
