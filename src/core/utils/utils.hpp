#ifndef utils
#define utils

#include <string>
#include <vector>

namespace Utils {
std::vector<std::vector<std::string>> filter(
    std::vector<std::vector<std::string>> data,
    std::string filter);
}  // namespace Utils

#endif